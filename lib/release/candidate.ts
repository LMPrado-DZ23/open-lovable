import {createHash} from 'node:crypto';
import {ProjectError} from '../projects/store';
export interface ReleaseCandidate {version:string;revision:string;artifactDigest:string;migrations:string[];sbom:string[];guidePath:string;rollbackRevision:string;optionalExtras:string[];status:'candidate';digest:string;}
export function createReleaseCandidate(input:{version:string;revision:string;artifact:Uint8Array;migrations:string[];sbom:string[];guidePath:string;rollbackRevision:string;optionalExtras:string[]}):ReleaseCandidate{if(!/^\d+\.\d+\.\d+$/.test(input.version)||!/^[a-f0-9]{7,64}$/.test(input.revision)||!/^[a-f0-9]{7,64}$/.test(input.rollbackRevision)||!input.artifact.byteLength||!input.guidePath.startsWith('docs/'))throw new ProjectError('Release candidate metadata is incomplete.',422);const artifactDigest=createHash('sha256').update(input.artifact).digest('hex');const value={...input,artifactDigest,status:'candidate' as const};return {...value,digest:createHash('sha256').update(JSON.stringify({...value,artifact:undefined})).digest('hex')};}
