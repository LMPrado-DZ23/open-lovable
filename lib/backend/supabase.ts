import {ProjectError} from '../projects/store';
import {assertNoSecret,type BackendBinding,type BackendCapabilities} from './contracts';
export interface SupabaseConnection {projectRef:string;credentialRef:string;schema:string;capabilities:BackendCapabilities;}
export function connectSupabase(projectId:string,environment:BackendBinding['environment'],projectRef:string,credentialRef:string,observed:{schema:string;health:BackendCapabilities['health'];auth:boolean;storage:boolean;functions:boolean}):BackendBinding {if(!/^[a-z0-9-]{6,80}$/.test(projectRef))throw new ProjectError('Invalid Supabase project reference.',400);assertNoSecret(credentialRef);const capabilities:BackendCapabilities={auth:observed.auth,storage:observed.storage,functions:observed.functions,database:true,schema:observed.schema,health:observed.health};return {projectId,environment,provider:'supabase',resourceId:projectRef,credentialRef,state:observed.health==='healthy'?'ready':'connected',capabilities};}
