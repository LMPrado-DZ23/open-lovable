import {ProjectError} from '../projects/store';
import type {Entitlement,UsageEvent,UsageLedger} from './ledger';
export function assertEntitlement(entitlement:Entitlement,event:UsageEvent,ledger:UsageLedger):void{if(!entitlement.active||entitlement.workspaceId!==event.workspaceId)throw new ProjectError('Workspace entitlement is inactive or mismatched.',403);if(event.source==='byok')return;const limit=entitlement.limits[event.operation];if(typeof limit!=='number')throw new ProjectError('Usage limit is unknown; operation is blocked.',409);if(ledger.usage(event.workspaceId,event.operation,'platform')+event.units>limit)throw new ProjectError('Workspace usage limit exceeded.',429);}
export function entitlementFor(workspaceId:string,plan:Entitlement['plan']):Entitlement{const common={ai:10000,sandbox:1000,storage:100000,research:1000,media:100,telephony:100};return {workspaceId,plan,active:true,limits:plan==='free'?{ai:100,sandbox:10,storage:1000,research:10,media:5,telephony:0}:plan==='pro'?common:{...common,ai:100000,sandbox:10000,storage:1000000,research:10000,media:1000,telephony:1000}};}
