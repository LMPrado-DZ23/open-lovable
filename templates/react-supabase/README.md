# React/Supabase app template

This generated-app template declares signup, login, logout, and password recovery flows. Its application session is separate from the Studio session. Tenant ownership is server-controlled, records are scoped by tenant, and writes use optimistic version checks. The manifest reports only capabilities actually configured; storage and functions remain disabled until their adapters are provisioned and verified.
